name: Publish model economy recalibration

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  parked:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Publication workflow parked while the reviewed economy pass is staged."
